//read 10 numbers ,find which of them have the biggest value ,and try to code it with only defining two variables ?
#include<iostream>
using namespace std;
int main(){
    int result,num;
    cin>>result;
    cin>>num; if(result<num)result=num;
     cin>>num; if(result<num)result=num; 
     cin>>num; if(result<num)result=num;
      cin>>num; if(result<num)result=num;
       cin>>num; if(result<num)result=num;
        cin>>num; if(result<num)result=num;
         cin>>num; if(result<num)result=num;
          cin>>num; if(result<num)result=num;
          cout<<result;
          return 0;
          
}